<!doctype HTML>

<head>
    <title>test</title>
</head>

<body>
    <h1>
        hello world
    </h1>

    <h2>
        dynamic content: {{content}}
    </h2>

    <p>
        {% if flag == True %}
            'tis true
        {% else %}
            'tis false
        {% endif %}
        
    </p>

    <p>
        {% for x in range(10) %}
             {{x}}&nbsp;
        {% endfor %}
    </p>
    
</body>